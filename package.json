{
  "name": "portfolio",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "NODE_NO_WARNINGS=1 lerna run dev --parallel",
    "build": "NODE_NO_WARNINGS=1 lerna run build --parallel",
    "test": "bun run build && NODE_NO_WARNINGS=1 lerna run test --parallel",
    "lint": "bun run build && NODE_NO_WARNINGS=1 lerna run lint --parallel"
  },
  "dependencies": { "bun": "^1.1.20" },
  "devDependencies": {
    "lerna": "^8.1.6"
  }
}
